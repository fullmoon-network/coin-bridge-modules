# 운영자 심사 체크리스트

모듈 PR을 리뷰할 때 이 순서로 확인해요.

## 1. 자동 검사 (CI가 대신)

- [ ] `node --check` 문법 통과
- [ ] 파일 크기 500줄 이하

## 2. 수동 검사

- [ ] **import 화이트리스트**: `discord.js`, `node:` 빌트인만. 서드파티
      패키지 import가 있으면 거절.
- [ ] **금지 패턴**: `eval`, `new Function`, `child_process`, `fetch(`,
      `process.env`, 동적 `import(변수)`, 난독화 흔적
- [ ] **DB 직접 접근 없음**: `db.js`나 pg를 import하지 않음
- [ ] **경제 쓰기 없음**: 잔액 지급/차감 로직이 없음 (읽기만 OK)
- [ ] **무한 루프 없음**: `while(true)`나 무한 재귀
- [ ] **에러 처리**: try/catch로 감싸고, catch에서 운영자에게 알림

## 3. 기능 검토

- [ ] 커맨드 이름이 기존 빌트인과 충돌하지 않음
- [ ] 설명이 실제 기능과 일치
- [ ] 옵션이 적절히 검증됨
- [ ] 스팸/어뷰즈 가능성 낮음
